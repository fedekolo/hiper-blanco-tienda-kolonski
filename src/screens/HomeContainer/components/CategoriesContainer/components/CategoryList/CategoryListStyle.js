export const categoryListStyle = theme => {
    return ({
        container: {
            display: "flex",
            gap: "2rem",
            flexWrap: "wrap",
            justifyContent: "center"
            }
        }
    )};