export const cartWidgetStyle = theme => {
    return ({
        btn: {
            '&:hover': {
                color: 'var(--color-C)',
                cursor: 'pointer',
                transform: 'scale(1.1)'
            }
        }
    })
};